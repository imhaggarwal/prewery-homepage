import React from 'react';

const TopBar = () => (
  <div className="top-bar">
    <span>🔥 Register with Prewery & connect with brands. <a href="#register">REGISTER</a></span>
  </div>
);

export default TopBar;